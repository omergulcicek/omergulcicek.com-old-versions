{
  "framework": "gatsby",
  "buildCommand": "npm run build",
  "outputDirectory": "public",
  "installCommand": "npm install",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet . && exit 1 || exit 0"
}
